# Security policy

Report vulnerabilities privately through GitHub's security advisory feature.
Do not open a public issue containing exploit details or sensitive data.

Only the latest release line receives security fixes. This server is read-only
and enforces a 20,000-character input limit, but operators must still apply
normal process isolation and dependency updates. Optional model downloads are
controlled by the Transformers/Hugging Face environment and are not vendored.
